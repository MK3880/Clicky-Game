import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>ヽ(=^･ω･^=)丿 Cat Clicky Game!</h1>
        <p>Click on an image to earn points, but don't click the same image more than once!</p>
    </div>
);

export default Title;

